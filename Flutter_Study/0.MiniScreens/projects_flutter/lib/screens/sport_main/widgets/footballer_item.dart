import 'package:flutter/material.dart';

class FootballerItem extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/Mbappe.png',
      fit: BoxFit.cover,
    );
  }
}
