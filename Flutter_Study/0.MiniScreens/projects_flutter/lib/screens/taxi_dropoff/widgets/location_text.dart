import 'package:flutter/material.dart';

class LocationText extends StatelessWidget {
  final String streatLocation;
  final String city;
  const LocationText({
    Key? key,
    required this.streatLocation,
    required this.city,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
