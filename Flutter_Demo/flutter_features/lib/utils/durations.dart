/// Constants for durations.
class KDuration {
  static const Duration durationShortExtraExtra = Duration(milliseconds: 50);
  static const Duration durationShortExtra = Duration(milliseconds: 100);
  static const Duration durationShort200 = Duration(milliseconds: 200);
  static const Duration durationShort250 = Duration(milliseconds: 250);
  static const Duration durationShort = Duration(milliseconds: 300);
  static const Duration durationDefault = Duration(milliseconds: 500);
  static const Duration durationMedium600 = Duration(milliseconds: 600);
  static const Duration durationMedium = Duration(milliseconds: 1000);
  static const Duration durationLong = Duration(milliseconds: 2000);
  static const Duration durationLongExtra = Duration(milliseconds: 4000);
}
