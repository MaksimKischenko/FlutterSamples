import 'package:flutter/material.dart';

class GoodsItem extends StatelessWidget {
  const GoodsItem({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
      
    );
  }
}